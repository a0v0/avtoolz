import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.avtoolz.app',
  appName: 'aVToolz',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
