version: 3

includes:
  app:merge-pdf:
    taskfile: ./apps/merge-pdf/Taskfile.yml
    dir: ./apps/merge-pdf
    # internal: true

tasks:
  default:
    silent: true
    cmds:
      - task -l

  install:
    desc: Install dependencies
    cmds:
      - yarn install

  start:
    desc: Start complete website
    cmds:
      - yarn start
